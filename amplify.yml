version: 1
backend:
  phases:
    build:
      commands:
        - npm install
        # IDとブランチを直接指定して、迷いなくバックエンドを構築させます
        - npx ampx pipeline-deploy --branch main --app-id d1mxmf2dr4vllz
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: out
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
